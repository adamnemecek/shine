{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(Windows) Launch",
            "type": "cppvsdbg",
            "request": "launch",
            "preLaunchTask": "build",
            "program": "${workspaceRoot}/target/debug/${workspaceRootFolderName}",
            "stopAtEntry": false,
            "externalConsole": true
        }
    ]
}